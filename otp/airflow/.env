# Recommended: set to your host user's UID. On Windows Docker Desktop 50000 is commonly used.
AIRFLOW_UID=50000

# Common DB / Airflow defaults used by the official docker-compose
POSTGRES_USER=airflow
POSTGRES_PASSWORD=airflow
POSTGRES_DB=airflow

# Optional: add additional envs if your docker-compose.yml references them
# AIRFLOW__CORE__EXECUTOR=LocalExecutor
# _PIP_ADDITIONAL_REQUIREMENTS=
